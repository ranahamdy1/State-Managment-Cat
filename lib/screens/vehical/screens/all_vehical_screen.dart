import 'package:flutter/material.dart';

class AllVehical extends StatelessWidget {
  const AllVehical({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Vehical"),
        ),
        body: Container(
          child: Text("Hi"),
        ));
  }
}
