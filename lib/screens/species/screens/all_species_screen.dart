import 'package:flutter/material.dart';

class AllSpecies extends StatelessWidget {
  const AllSpecies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("species"),
      ),
      body: Text("Hi"),
    );
  }
}
